.DS_Store

# Your real searches live here and are never committed (the dir ships empty via .gitkeep).
roles/*
!roles/.gitkeep

# Local secrets and the legacy private layer from the old app — never commit.
.env*
yours/

# subagent-driven-development progress ledger (scratch)
.superpowers/
